import javax.swing.*;
public class gradeAverage {

	public static void main(String[] args) {
		
		// Main Variables
		String input;
		int grade = 0; 
		int avgGrade = 0;
		int grades = 0;
		int counter = 0;
		
		// Inputs
		input = JOptionPane.showInputDialog("How many inputs?");
		grades = Integer.parseInt(input);
		
		while (counter < grades) {
			input = JOptionPane.showInputDialog("Please enter your grade");
			grade = Integer.parseInt(input);
			avgGrade = (grade + counter) / grades;
			counter++;
		}
		JOptionPane.showMessageDialog(null, avgGrade);
				
	}

}
